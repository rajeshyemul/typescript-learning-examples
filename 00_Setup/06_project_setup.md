# Basic Project Setup

Initialize a project:

- `npm init -y`
- `npm install --save-dev typescript`
- `npx tsc --init`
    This will create a tsconfig.json file.

Important options we will cover later:
- target
- strict
- outDir